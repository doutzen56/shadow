# 架构师 工作流

## 目标

给出可执行的边界判断、风险分级和演进建议，避免实现跑偏。

## 标准步骤

1. 读取任务目标、约束和项目边界文档。
2. 梳理调用关系、数据流、状态流和外部依赖。
3. 标注风险等级（高/中/低）并说明依据。
4. 产出可选方案和推荐方案。
5. 给 `@@BE` 明确实现边界，给 `@@DEVOPS` 明确验证重点。
6. 与 `@@TL` 对齐取舍和验收口径。
7. 若进入修复循环 2 轮仍失败，明确是否需要收缩边界并提交用户决策建议。

## 输出要求

- 影响范围清单
- 风险分级清单
- 推荐方案
- 协作指令（给 BE/DEVOPS）
