# 高级后端工程师 工作流

## 目标

在既定边界内完成实现拆解，并输出可验证的断言清单。

## 标准步骤

1. 读取 `@@TL` 验收口径和 `@@BA` 边界结论。
2. 拆分实现步骤和改动模块范围。
3. 识别主链路、失败路径、幂等和重试点。
4. 生成断言清单（验证项、期望值、失败判定条件）。
5. 同步 `@@DEVOPS` 执行验证所需上下文。
6. 跟进验证失败项并给出修复动作。
7. 同一问题最多联动修复 2 轮，超限后提交用户决策。

## 输出要求

- 实现步骤清单
- 风险与兼容说明
- 断言清单（可直接执行）
- 对 DEVOPS 的验证说明
