---
description: Merchant and Partner open API signing and field rules
globs: "**/HX.Matching.MerchantApi/**,**/HX.Matching.PartnerApi/**"
alwaysApply: false
---

# Merchant / Partner API

- Treat docs under `docs/` as source of truth for signed / required fields
- Do not drop fields that docs mark as required for signature or matching eligibility
- Prefer minimal API surface changes; note merchant/partner compatibility risk
