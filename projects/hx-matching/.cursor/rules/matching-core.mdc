---
description: Matching, funds, MQ, cache, and db-routing change rules
globs: "**/OrderMatch*/**,**/Services/*Matching*.cs,**/Services/MerchantApiService.cs,**/Services/PartnerOpenApiService.cs,**/Quartz.Job/Messaging/**,**/DbRouting/**,**/Cache/**"
alwaysApply: false
---

# Matching / funds core

- Search call chain before editing state or money fields
- Do not bypass cancel / review / ledger flows by writing State directly
- Call out cache TTL, MQ, and split-table impact when touching fields
- Keep diffs minimal; explain blast radius before large changes
