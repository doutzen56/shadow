---
description: 非必要不写同义反复、对照表式测试
alwaysApply: true
---

# 测试要证明行为

非必要不要写没什么用的测试。禁止为了“有测试”去测内部映射表。

不要写：

- 调 `ResolveX(类型)` 再断言等于旁边的 `Build()`（同义反复）
- 对硬编码白名单做 true/false 陪伴断言
- 把单测塞进 TestIntegration，却不打真实依赖
- 为了单测把 private 策略改成 public

要写就写能抓住回归的：真实 IO、失败路径、手工迁测（如 ES 两边 count / `_id`）。映射怎么写，看生产代码即可。
