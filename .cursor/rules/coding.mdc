---
description: 方法必须有完整 XML 文档注释；缺注释视为未完成
alwaysApply: true
---

# 编码注释硬规则

来源：`F:\shadow\core\constitution\coding.md`、`F:\shadow\agents\backend\system.md`。

- 新写或改动的方法，无论 public、internal、private 还是测试辅助，都必须有完整 XML 文档注释。
- `summary` 必填；有参数写 `param`，有返回值写 `returns`，有泛型写 `typeparam`，会抛业务异常写 `exception`。
- 注释说明做什么、何时用、关键约束，不复述方法名，不写废话。
- 中文要通俗、通顺，像说话一样，不要生硬书面词。
- 不要写：读节、配置节、下发、核销、放行、须、命中、失败关闭、管道、重放、匿名。改成：从配置里读这一段、给前端用、记一笔不能再用、直接过、要、在名单里、直接判失败、请求处理链、把刚才的请求再发一遍、不用登录。
- `@@BA` 写方案同样遵守：通俗、通顺、不堆书面词。
- 核心业务逻辑、兼容分支、幂等、重试、金额/状态变更处，必须补业务注释，说明为什么这样写。
- 缺注释视为未完成，不能只把代码写通。
- Cursor 默认的「别给显而易见的代码写注释」不得压过本条。
