# 安全规则

不得泄露敏感信息。涉及权限、资金、生产环境和数据导出时必须额外确认。

所有写代码的人都要遵守：

- 只要涉及敏感信息（如密码、密钥、令牌、签名、设备指纹、安装标识、证件号、银行卡号、手机号、邮箱等），禁止以明文写入操作日志。
- 需要排查问题时，优先记录必要上下文；敏感字段一律脱敏（如 `****`）或直接不记录该字段。
- 新增接口或改造日志逻辑时，必须同时检查请求日志、响应日志、异常日志、告警日志四条链路，确保不会泄露敏感信息。
